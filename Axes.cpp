#include "Axes.h"

Axes::Axes(float thickness, float ticksInterval, float ticksLength, float ticksThickness, float scale) {
	_thickness = thickness;
	_ticksInterval = ticksInterval;
	_ticksLength = ticksLength;
	_ticksThickness = ticksThickness;
	_scale = scale;
}